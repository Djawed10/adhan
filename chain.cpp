#include<stdio.h>
#include<conio.h>

int main(){
	
   char *str[] = {"page 1","page 2", "page3","page4","page5"};
   char *nom = "ali";
  
  printf("premie element de tablau est %s. \n", str[0]);
   printf("dexiem element de tablau est %s.\n", str[1]);
   printf("troisiem element de tablau est %s.\n", str[2]);
   printf(" quatrieme  element de tablau est %s.\n", str[3]);
    printf(" cinquime element de tablau est %s.\n", str[4]);
    
    
char ch;
int current_n=0;
    while (1) {
 
        if ( kbhit() ) {
 
            // Stores the pressed key in ch
            ch = getch();
 printf("\nKey pressed= %c ", ch) ;
 
 
 
       
        	 
        	 
        	 if(ch=s){
        	 	if(current_n=str[4]){
				  printf("premie element de tablau est %s. \n", str[0]);
				 }
				 printf("")
				 
				 
        
			 }
        	
        	
        	
        	
        	
		}
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
           	
           return 0;
       }
   }
}